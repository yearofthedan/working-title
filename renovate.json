{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "schedule": ["every weekend"],
  "prHourlyLimit": 0,
  "minimumReleaseAge": "3 days",
  "packageRules": [
    {
      "description": "Group all minor/patch devDependencies into one PR",
      "matchDepTypes": ["devDependencies"],
      "matchUpdateTypes": ["minor", "patch"],
      "groupName": "all dev dependencies"
    },
    {
      "description": "Group all minor/patch production dependencies into one separate PR",
      "matchDepTypes": ["dependencies"],
      "matchUpdateTypes": ["minor", "patch"],
      "groupName": "all production dependencies"
    },
    {
      "description": "Do not upgrade ajv: the upgrade breaks ESLint 10 draft-04 resolution. ReDoS advisory GHSA-2g4f-4pwh-qvx6 is accepted — dismiss it in GitHub > Security > Dependabot alerts to silence any security PR.",
      "matchPackageNames": ["ajv"],
      "enabled": false
    }
  ]
}
